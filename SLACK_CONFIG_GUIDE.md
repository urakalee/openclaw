# 写在前面：搞个 AI 助手玩玩 👋

兄弟们，最近在折腾 OpenClaw，这玩意儿能把 AI 接入 Slack、飞书这些聊天软件，还挺好用的。配置 Slack 的时候踩了不少坑，总结了个**“一次过”**的避坑指南，给大伙儿省点时间。

顺便提两句部署的事儿：
*   **服务器**：直接整**腾讯云**那个 **99块钱一年** 的轻量服务器就行，性价比贼高，搭这个绰绰有余。
*   **模型**：国内同学强推 **MiniMax**（名之梦）。这可是 OpenClaw 官网推荐的，国内直连速度飞快，不用梯子，注册还送积分，白嫖真香。刚才试了下语音功能，效果很顶。
*   **飞书**：如果不爱用 Slack，它也支持飞书。腾讯云官网上好像有飞书接入的记录/教程，感兴趣的自己去搜搜看，我没试，交给你们探索了。

下面是 Slack 的傻瓜式配置流程 👇

---

# OpenClaw Slack 配置指南 (最佳实践版)

## 1. 创建 Slack App

1. 登录 [Slack API 后台](https://api.slack.com/apps)。
2. 点击 **"Create New App"** -> **"From scratch"**。
3. 输入 App 名称（例如 "Davinci"）并选择工作区。

## 2. 开启 Socket Mode (获取 App-Level Token)

*建议最先做这一步，这样配置事件订阅时就不需要提供 URL 了。*

1. 左侧点击 **"Socket Mode"**，打开 **"Enable Socket Mode"**。
2. 在弹窗中输入 Token 名称（如 "Socket Token"）。
3. 点击 **"Generate"**。
4. **复制 `xapp-` 开头的 Token**（这是 **App-Level Token**）。
5. 点击 "Done"。

## 3. 配置事件订阅 (Event Subscriptions)

*这一步会自动添加部分所需的“读权限”，省去手动查找的麻烦。*

1. 左侧点击 **"Event Subscriptions"**，打开 **"Enable Events"**。
2. 展开 **"Subscribe to bot events"**，添加以下事件：
   - `message.im` (接收私信)
   - `app_mention` (接收群组 @消息)
   *此时系统会自动提示添加对应的 User Scope，无需手动操作。*
3. 点击底部的 **"Save Changes"**。

## 4. 补充权限 & 开启聊天 (OAuth & App Home)

*事件订阅不会自动添加“写权限”，必须手动补充。*

1. 左侧点击 **"OAuth & Permissions"**。
2. 滚动到 **"Bot Token Scopes"**，手动添加以下权限：
   - `chat:write` (发送消息 - 必须)
   - `files:write` (发送文件/文档 - 推荐)
   - `im:write` (主动发起私信 - 推荐)
3. 左侧点击 **"App Home"**。
4. 在 **"Show Tabs"** -> **"Messages Tab"** 下，**务必勾选 "Allow users to send Slash commands and messages..."**。
   *(如果不做这一步，聊天窗口会显示“发送功能已关闭”，用户无法发消息)*

## 5. 安装应用 (获取 Bot Token)

*所有配置完成后，再一次性安装，避免反复 Reinstall。*

1. 回到 **"OAuth & Permissions"** 页面。
2. 滚动到顶部，点击 **"Install to Workspace"** 并授权。
3. **复制 `xoxb-` 开头的 Token**（这是 **Bot User OAuth Token**）。

## 6. 配置 OpenClaw

将两个 Token 填入配置：
- `botToken`: 第 5 步获取的 `xoxb-...`
- `appToken`: 第 2 步获取的 `xapp-...`

---

## 💡 小贴士

*   **关于群聊**：如果想在群里使用，记得先 `/invite` 机器人进群。要是 @它 没反应，可能是群聊权限策略的问题，这事儿简单，**直接让 OpenClaw 自己调整一下配置就行**（跟它说一声“把群聊策略改成 open”）。

---
*Generated by Davinci*
